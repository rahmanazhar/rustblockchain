apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: rustchain-validator-pdb
  namespace: rustchain
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: rustchain
      component: validator
